{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "vercel#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "src/util/constants.ts"]
    },
    "test-unit": {
      "dependsOn": ["$TEST_FILES", "build"],
      "outputs": []
    }
  }
}
