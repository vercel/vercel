[project]
name = "basic"
version = "0.1.0"
description = "Basic FastAPI example for vercel-workers"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "vercel-workers",
]

[tool.uv.sources]
vercel-workers = { workspace = true }
