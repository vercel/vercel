{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!docs/**"],
      "outputs": ["**/*.js", "**/*.d.ts", "**/*.md"]
    }
  }
}
