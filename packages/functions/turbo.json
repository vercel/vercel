{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_EXTENDS$", "!docs/**"],
      "outputs": ["**/*.js", "**/*.d.ts"]
    }
  }
}
