// Generated by ts-to-zod
import { z } from 'zod';

import { dependencySourceSchema } from './../requirement/schema.zod';

export const uvConfigWorkspaceSchema = z.object({
  members: z.array(z.string()).optional(),
  exclude: z.array(z.string()).optional(),
});

export const uvIndexEntrySchema = z.object({
  name: z.string(),
  url: z.string(),
  default: z.boolean().optional(),
  explicit: z.boolean().optional(),
  format: z.string().optional(),
});

export const uvConfigSchema = z.object({
  sources: z
    .record(z.union([dependencySourceSchema, z.array(dependencySourceSchema)]))
    .optional(),
  index: z.array(uvIndexEntrySchema).optional(),
  workspace: uvConfigWorkspaceSchema.optional(),
});
