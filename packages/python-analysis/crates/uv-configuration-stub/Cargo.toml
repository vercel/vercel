# Stubbed uv-configuration: provides PackageNameSpecifier, NoBinary, NoBuild
# for uv-requirements-txt --no-binary/--only-binary parsing.
[package]
name = "uv-configuration"
version = "0.0.20"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0 OR MIT"

[lib]
doctest = false

[dependencies]
uv-normalize.workspace = true
