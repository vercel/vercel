# Stubbed uv-client: provides only BaseClientBuilder and Connectivity
# for uv-requirements-txt in WASM (always offline, no HTTP).
[package]
name = "uv-client"
version = "0.0.20"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0 OR MIT"

[lib]
doctest = false

[dependencies]

[features]
default = []
