[package]
name = "standalone-std"
version = "0.1.0"
edition = "2021"

# No dependencies on purpose: this fixture is the fast smoke test for standalone
# mode, so it must not pay for a crates.io resolve or a dependency compile.
[dependencies]
