{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "vercel_runtime#build"],
      "outputLogs": "new-only",
      "outputs": ["dist/**", "bin/**"]
    },
    "test-e2e-artifacts": {
      "command": ["pnpm", "run", "test-e2e"]
    }
  }
}
