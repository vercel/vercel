{
  "name": "26-multiple-lock-files-pnpm",
  "scripts": {
    "build": "mkdir -p public && (printf \"pnpm version: \" && pnpm -v) > public/index.txt"
  },
  "dependencies": {
    "once": "^1.4.0"
  }
}
