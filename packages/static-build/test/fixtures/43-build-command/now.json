{
  "builds": [
    {
      "src": "build.js",
      "use": "@vercel/static-build",
      "config": {
        "zeroConfig": true,
        "buildCommand": "node build.js"
      }
    }
  ],
  "probes": [{ "path": "/", "mustContain": "Time of Creation:" }]
}
