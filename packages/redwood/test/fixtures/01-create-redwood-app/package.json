{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "scripts": {
    "build": "rw db up --no-db-client --auto-approve && rw build"
  },
  "devDependencies": {
    "@redwoodjs/core": "0.15.0"
  },
  "engines": {
    "node": "16.x",
    "yarn": ">=1.15"
  }
}
