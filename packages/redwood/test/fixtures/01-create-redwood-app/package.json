{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "scripts": {
    "build": "rw db up --no-db-client --auto-approve && rw build"
  },
  "devDependencies": {
    "@redwoodjs/core": "0.15.0"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
