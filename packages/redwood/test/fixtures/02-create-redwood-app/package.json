{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "scripts": {
    "build": "rw build && rw db up --no-db-client --auto-approve && yarn rw dataMigrate up"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.15.3",
    "netlify-plugin-prisma-provider": "^0.3.0"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
