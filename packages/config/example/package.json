{
  "private": true,
  "scripts": {
    "generate-config": "npx @vercel/config",
    "build": "npm run generate-config"
  },
  "dependencies": {
    "@vercel/config": "file:../"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsx": "^4.20.6",
    "typescript": "^4.9.0"
  }
}
