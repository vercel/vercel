List deployments for the currently linked project using the Vercel CLI.

Use `vercel list` or `vc list` in non-interactive mode. Use JSON output when available, and save the command output to `deployment-list-output.json`.

The project is already linked. If there are no deployments, the saved file should still contain valid JSON output from the CLI for this linked project.
