Link this directory to Vercel (if needed) and build it. No interactive input—use flags so commands complete without prompting.

When done, write the exact single CLI command you used to run the build (one line) to `command-used.txt`.
