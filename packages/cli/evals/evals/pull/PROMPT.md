Pull the latest project settings and development environment variables for this linked project using the Vercel CLI.

Use `vercel pull` or `vc pull` with non-interactive flags so it completes without prompts. Save a short note about what happened to `pull-summary.txt`.

The project is already linked, and the expected result is that `.vercel/project.json` still exists and `.vercel/.env.development.local` is created or refreshed.
