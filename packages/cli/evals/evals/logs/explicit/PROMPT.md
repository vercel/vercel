Create a preview deployment for this fixture, then use `vercel logs` or `vc logs` to fetch request logs for it.

Use a bounded logs mode so the command exits. Save the deployment URL in `logs-deployment-url.txt` and save the logs output in `logs-output.txt`.
