Use `vc curl /` to make a request to this project's deployment.

Use `--yes` so it completes without prompts.

Save the deployment URL you requested in `curl-deployment-url.txt` and save the response body in `curl-response.txt`.
