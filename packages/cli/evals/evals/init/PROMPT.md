Use `vercel init` or `vc init` to initialize a new Next.js project in a sub-folder called `project`.

Choose a TypeScript Next.js template so the project includes `next.config.ts`.

After initialization, write a short summary of what you created in `init-summary.txt`.
