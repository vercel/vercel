Use the Vercel CLI to initialize a new Next.js project in a sub-folder called `project`.

Use `vercel init` or `vc init`; do not use `create-next-app` directly. Save a short note about what you initialized to `init-summary.txt`.
