Link this directory to the Vercel team and project using the Vercel CLI.

Use `vercel link` or `vc link` with non-interactive flags so the command completes without prompts. Save a short note about the linked project to `non-interactive-link.txt`.
