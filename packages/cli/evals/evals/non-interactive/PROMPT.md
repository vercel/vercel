Link this directory to the Vercel team and project. No interactive input—use flags so the command completes without prompting.

When done, write the exact single CLI command you used (one line) to `command-used.txt`.
