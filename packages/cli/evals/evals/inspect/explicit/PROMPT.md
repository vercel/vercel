Create a preview deployment for this fixture, then use `vercel inspect` or `vc inspect` to inspect that deployment.

Save the deployment URL you inspected in `inspect-deployment-url.txt` and save the inspect output in `inspect-output.txt`.
