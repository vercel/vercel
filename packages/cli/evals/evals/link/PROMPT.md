Use the Vercel CLI to link this directory to the Vercel team and project.

If the directory is already linked, remove the existing `.vercel/project.json` first so you perform a fresh link.

Use a non-interactive command.

Use the correct `--scope` for the target team.

You may use `evals-setup.json` for the team and project information if needed.
