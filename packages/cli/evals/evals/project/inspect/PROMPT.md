Inspect the currently linked Vercel project using the Vercel CLI.

Use `vercel project inspect` or `vc project inspect` in non-interactive mode. Save the useful result, including the project name or project id, to a file called `project-inspect.txt`.

The project is already linked. You can read `.vercel/project.json` if you need the expected project id or team id.
