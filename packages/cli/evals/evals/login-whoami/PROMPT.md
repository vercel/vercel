Use `vercel whoami` or `vc whoami` with JSON output to determine which account is currently authenticated.

Save the JSON output in `whoami-output.json`.

If no one is signed in, use the available credentials to authenticate and then check again.
