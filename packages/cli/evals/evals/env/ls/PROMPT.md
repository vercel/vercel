List this project's environment variables using the Vercel CLI.

Use `vercel env ls --format=json` or `vc env ls --format=json`, and save the JSON output to `env-ls-output.json`. The project is already linked.
