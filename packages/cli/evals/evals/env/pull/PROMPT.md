Pull this project's environment variables to a file using the Vercel CLI (e.g. `vercel env pull` or `vercel env pull .env.local`).

Use non-interactive flags if needed so the command completes without prompts (e.g. `--yes` when overwriting an existing file).
