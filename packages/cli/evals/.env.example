# Copy to .env when you add evals and run pnpm test.
# See @vercel/agent-eval docs for options.

# Agent (choose one):
# AI_GATEWAY_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Sandbox (same as test.yml / test-e2e: long-lived token):
# VERCEL_TOKEN=
