{
  "name": "app-2",
  "version": "0.0.1",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "mkdir -p dist && echo 'module.exports = \"world\"' > dist/index.js"
  },
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-script",
        "options": {
          "script": "build"
        },
        "dependsOn": [
          "^build"
        ]
      }
    }
  }
}
