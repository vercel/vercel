{
  "version": 2,
  "name": "now-dev-next",
  "builds": [{ "src": "package.json", "use": "@now/next@canary" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index?route-param=b"
    }
  ]
}
