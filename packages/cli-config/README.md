# @vercel/cli-config

Shared utilities for Vercel [global configuration files](https://vercel.com/docs/project-configuration/global-configuration).

This package contains the pure config-path, parsing, defaulting, and file read/write helpers used by packages that need access to global configration.
