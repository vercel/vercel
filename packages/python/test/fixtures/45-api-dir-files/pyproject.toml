[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.12"
classifiers = [
  "Private :: Do Not Upload",
]
dependencies = [
    "uvicorn>=0.24",
    "werkzeug>=1.0.1",
    "fastapi>=0.125.0",
    "flask>=3.1.2",
    "starlette>=0.27.0",
]
