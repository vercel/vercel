[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.12"
classifiers = [
  "Private :: Do Not Upload",
]
dependencies = [
    "fastapi==0.121.1",
    "uvicorn>=0.24",
    "werkzeug>=1.0.1",
]
