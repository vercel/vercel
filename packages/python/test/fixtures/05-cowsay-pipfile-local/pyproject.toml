[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.12"
classifiers = [
  "Private :: Do Not Upload",
]
dependencies = [
    "asgiref>=3.0.0",
    "cowpy>=1.1.0",
    "starlette>=0.27.0",
    "uvicorn>=0.24",
    "werkzeug>=1.0.1",
]
