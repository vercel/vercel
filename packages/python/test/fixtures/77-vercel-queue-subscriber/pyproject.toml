[project]
name = "vercel-queue-subscriber"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "vercel",
    "vercel-queue",
]

[[tool.vercel.subscribers]]
entrypoint = "tasks"
