{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["**/*.js", "**/*.d.ts"]
    },
    "build:docs": {
      "dependsOn": ["build", "^build"]
    }
  }
}
