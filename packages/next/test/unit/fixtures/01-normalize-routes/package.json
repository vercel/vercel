{
  "dependencies": {
    "next": "canary",
    "react": "experimental",
    "react-dom": "experimental"
  },
  "scripts": {
    "build": "node -e \"console.log(process.version,process.cwd(),require('fs').readdirSync('./node_modules/.bin'))\" && next build"
  }
}
