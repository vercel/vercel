{
  "name": "@vercel/related-projects",
  "version": "1.0.0",
  "description": "Vercel utilities for working with related projects",
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/vercel/vercel.git",
    "directory": "packages/related-projects"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "node ../../utils/build.mjs",
    "test": "jest --reporters=default --reporters=jest-junit --env node --verbose --runInBand --bail --coverage",
    "test-unit": "pnpm test",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "27.4.1",
    "@types/node": "14.18.33",
    "ajv": "^6.12.3",
    "jest-junit": "16.0.0",
    "typescript": "4.9.5"
  },
  "optionalDependencies": {
    "ajv": "^6.12.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
