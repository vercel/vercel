{
  "private": true,
  "name": "@vercel-internals/ipc-proxy",
  "version": "1.0.0",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "files": [
    "dist",
    "bin",
    "bootstrap"
  ],
  "scripts": {
    "build": "node build.mjs",
    "type-check": "tsc --noEmit",
    "test": "go -C bootstrap test ./... && vitest run --config ../../vitest.config.mts",
    "test-go": "go -C bootstrap test ./...",
    "test-unit": "go -C bootstrap test ./... && vitest run --config ../../vitest.config.mts",
    "vitest-run": "vitest -c ../../vitest.config.mts"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "20.11.0",
    "@vercel-internals/tsconfig": "workspace:*",
    "@vercel/build-utils": "workspace:*",
    "execa": "5",
    "fs-extra": "11.2.0",
    "tar": "7.5.11",
    "vitest": "2.0.3"
  }
}
