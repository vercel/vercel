{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": { "outputs": ["dist/**"] },
    "test": { "dependsOn": ["build", "//#generate:cache-keys"] },
    "test-unit": { "dependsOn": ["build", "//#generate:cache-keys"] }
  }
}
