To develop locally:

```
npm install
vc dev
```

```
open http://localhost:3000
open http://localhost:3000/health
```

To build locally:

```
npm install
vc build
```

To deploy:

```
npm install
vc deploy
```
