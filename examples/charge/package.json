{
  "name": "charge-example",
  "version": "1.0.0",
  "scripts": {
    "start": "charge serve source",
    "dev": "charge serve source --port $PORT",
    "build": "charge build source public"
  },
  "devDependencies": {
    "@static/charge": "^1.5.0"
  }
}
