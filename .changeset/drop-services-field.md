---
'@vercel/build-utils': patch
'@vercel/client': patch
'@vercel/fs-detectors': patch
'vercel': patch
---

[services] Remove the `services` field from `vercel.json` and the `VERCEL_USE_SERVICES` gate.
