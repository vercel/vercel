---
'@vercel/backends': patch
---

Detect the entrypoint after the install and build commands run, matching the wrapper builders. Projects whose entrypoint is generated by the Build Command (e.g. `package.json#main` pointing into `dist/`) no longer fail with "no entrypoint found".
