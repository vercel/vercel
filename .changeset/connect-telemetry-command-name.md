---
'vercel': patch
---

Emit `connect` as the telemetry command name for `vercel connect`, and track the previously-missing `create` service argument, `--name` and `--format`, plus all `token` arguments and options.
