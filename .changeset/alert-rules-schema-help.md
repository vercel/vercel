---
"vercel": patch
---

Add `vc alerts rules schema` and rule creation examples for built-in and custom alert rules. Use an explicit `--project` as the built-in rule target, and include complete custom alert project metadata for dashboard editing.
