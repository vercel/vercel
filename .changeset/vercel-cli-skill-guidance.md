---
---

Clarify Vercel CLI skill guidance: structured-output flags (`vercel api --raw`, `--format json` over deprecated `--json`), the stdout/stderr stream contract including help output, agent non-interactive defaults and agent-output JSON, no-cache deploy workflows, build-failure investigation, and `vercel list` pagination.
