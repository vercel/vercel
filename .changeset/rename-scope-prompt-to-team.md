---
'vercel': patch
---

Rename the `vc` setup prompt from `Which scope should contain your project?` to `Team?`. "Scope" was CLI-only jargon; the dashboard, docs, and billing all use "Team" (which also covers personal accounts). Shorter, clearer, matches the language users already see everywhere else.
