---
'vercel': minor
---

Add `vercel dns configure --resend <file>` to add missing DNS records from a Resend domain JSON response. Supports previews, non-interactive confirmation, conflict checks, and reruns without replacing existing DNS records.
