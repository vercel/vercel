---
'vercel': patch
---

Record Builder versions in a `builders` manifest in package.json (pinned to exact workspace versions at pack time) and prefer it over `peerDependencies` for Builder version resolution
