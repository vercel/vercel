---
'vercel': patch
---

Add `vercel env shared add` to create team Shared Environment Variables (targets, project links, sensitive type, comment). Values are read from an argument, stdin, or a prompt and are never printed.
