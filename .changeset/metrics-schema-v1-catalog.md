---
'vercel': minor
---

List custom metrics and their observed attributes from the Observability API catalog and query them through the `/metrics/v1` API proxy, while preserving the existing schema and query paths for `vercel.*` metrics.
