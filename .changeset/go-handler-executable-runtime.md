---
'@vercel/go': patch
---

Run Go functions as executable HTTP servers through the shared IPC proxy, with response streaming, configured function architectures, `WaitUntil`, request context, structured logs, and outbound fetch metrics through the new Go runtime SDK.
