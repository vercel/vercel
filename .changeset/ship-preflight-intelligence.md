---
'vercel': patch
---

`vercel ship`: inject the CLI's deterministic deployment intelligence into the mission preflight — detected frameworks per directory, workspace manager, inferred services, deployment-intent files, and the teams `--scope` accepts — and tighten the mission with a pre-verified command reference, explicit scope pinning, and output budgets. Measured against a real session, these were the dominant sources of avoidable wall time: CLI flag trial-and-error, a resource provisioned in the wrong scope, and re-discovery of facts the CLI can compute statically.
