---
---

Restructure the vercel-cli agent skill around the installed CLI as the syntax authority: handwritten references carry only behavioral guidance, a generated command map covers discovery, and CI validates every documented example against real command metadata.
