---
'@vercel/client': minor
'vercel': minor
---

Add opt-in zstd deployment archive compression for the Vercel CLI. Using `--archive=zstd` or `--compress=zstd` requires the `zstd` binary to be installed on `PATH`; `tgz` remains the portable default.
