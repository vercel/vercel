---
'vercel': minor
---

Add `vercel routes` command for managing project-level routing rules. This includes:
- `routes list` - List all routing rules
- `routes list-versions` - List version history
- `routes inspect <name-or-id>` - Show detailed info for a specific route
