---
'vercel': patch
---

Add Command Code support to `vercel ai-gateway coding-agents setup`. The setup command writes an environment-backed Vercel AI Gateway provider to `~/.commandcode/providers.json`, preserves existing BYOK providers, and seeds a model for Command Code's `/model` picker.
