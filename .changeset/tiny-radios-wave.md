---
'@vercel/python': patch
---

Wire the in-repo `vercel-workers` Python package into CI/release workflows and preview wheel artifacts. Also allow `@vercel/python` builds to consume a PR wheel override via `VERCEL_WORKERS_PYTHON`.
