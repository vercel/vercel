---
'vercel': patch
---

Improve `vercel ai-gateway coding-agents setup` prompts: use the searchable team picker, keep the multiselect key legend visible after the first selection, narrow the prompt lines by moving context off the question line, and replace the apply confirmation with an Apply / Copy prompt / Cancel choice ("Copy prompt" copies agent-ready setup instructions to the clipboard and is available only when the key is stored in the macOS Keychain).
