---
'vercel': patch
---

Refine setup-and-link prompt copy: `"Want to modify these settings?"` → `"Customize defaults?"` and `"Do you want to change additional project settings?"` → `"Customize Vercel Authentication?"`. Shorter, clearer, and the second prompt now names the actual setting it gates (Vercel Authentication) instead of vague "additional settings".
