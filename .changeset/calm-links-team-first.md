---
'vercel': patch
---

Ask interactive `vercel link` users to choose a team before project discovery.
Explicit `--scope` and `--team` values skip the team prompt and restrict project
lookup to that team.
