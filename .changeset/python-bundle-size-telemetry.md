---
'@vercel/python': patch
---

Report the full function footprint (source + venv + runtime-install tooling) as `python.bundle.totalSizeBytes`, and the shipped zip size as `python.bundle.shippedSizeBytes`.
