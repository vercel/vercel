---
'vercel': patch
---

Add `vercel comments` — a project-aware inbox for Vercel Toolbar comments. List unresolved comments for the linked project (focused on the current Git branch), inspect full threads, reply, react, resolve/reopen (optionally with a closing reply via `-m`), edit/delete messages, and browse reaction emoji names. Supports `--format json` with a stable envelope for scripting and agents.
