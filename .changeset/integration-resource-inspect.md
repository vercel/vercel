---
'vercel': minor
---

Add `vercel integration resource inspect <resource>` (alias `status`) to show a marketplace resource's details and live status fetched fresh from the provider, rather than the cached status shown by `vercel integration list`. Supports `--format=json`.
