---
'@vercel/build-utils': minor
'@vercel/fs-detectors': minor
'vercel': minor
---

Replace implicit `{SERVICE_NAME}_URL` injection in `experimentalServices` with an explicit `envVars` configuration.
