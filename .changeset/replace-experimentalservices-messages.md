---
'@vercel/fs-detectors': patch
---

Replace user-facing `experimentalServices` references with `services` in error and warning messages. The deprecated v1 config key should no longer appear in guidance shown to users.
