---
'@vercel/fs-detectors': minor
'@vercel/python': minor
'vercel': minor
---

Support Python Routing Middleware entrypoints through `proxy.entrypoint`, with optional path matching through `proxy.matcher`. Python proxy dependencies are isolated to the `proxy` dependency group in `pyproject.toml`.
