---
'vercel': patch
'@vercel/fs-detectors': patch
---

Improve `vercel link` services detection so inferred multi-service layouts can be adopted from a selected root directory, and expose the richer services detection result that powers the setup flow.
