---
vercel: minor
---

Add `vercel project access-group` to inspect a team access group by id or slug, and extend `vercel project access-groups` with search, limit, cursor pagination, and non-interactive JSON errors for dashboard parity.
