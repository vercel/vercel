---
'vercel': patch
---

Add `vercel env shared remove` to delete a team Shared Environment Variable, with TTY confirmation defaulting to No, `--yes`, and a non-interactive `confirmation_required` contract.
