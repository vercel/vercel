---
'vercel': minor
---

Add `--auto-install` to `vercel project web-analytics` to install `@vercel/analytics` with the detected package manager (npm, yarn, pnpm, or bun). When combined with `--format json`, the command includes `packageInstall` and dashboard-style `integrate` hints (docs URL and a Next.js layout example) for agents and CI.
