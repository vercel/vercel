---
'vercel': patch
---

Enable service route-prefix auto stripping for explicitly configured
multi-service `vercel.json` setups across runtimes, matching generated-prefix
behavior during build and dev orchestration.
