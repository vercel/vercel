---
vercel: minor
---

Dashboard CLI parity: add `getCliCommandSurface()` for automation, `vercel oauth-apps`, `vercel teams sso`, `vercel project protection`, `vercel integration installations`, `vercel alerts inspect`, and `vercel domains price`.
