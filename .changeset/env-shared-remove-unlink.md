---
'vercel': patch
---

Add `vercel env shared remove` and `vercel env shared unlink` to delete a team Shared Environment Variable (TTY confirmation defaulting to No, `--yes`, and a non-interactive `confirmation_required` contract) and to remove a single project link without deleting the variable.
