---
'@vercel/build-utils': minor
'vercel': patch
---

Add isPackageInstalled util for detecting dependencies during build. 
Fix Vercel Flags dependency detection for emitting datafiles during builds with OIDC tokens.
