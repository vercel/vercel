---
---

Add test coverage for `Containerfile` entrypoints in container service
detection (V1 + V2) and the `@vercel/container` build path. `Containerfile` was
already supported alongside `Dockerfile`; these tests lock in that behavior.
