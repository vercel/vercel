---
'vercel': patch
---

Shorten the `vc` setup prompt `What's your project's name?` to `Name?`. The directory-derived default still renders inline (`Name? (my-tan-test)`), so the answer is obvious without the apostrophe-laden long form.
