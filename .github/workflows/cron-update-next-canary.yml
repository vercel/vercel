- name: Run GitHub Script
  uses: actions/github-script@v7
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    script: |
      console.log('This is my script running successfully with a GitHub token.');